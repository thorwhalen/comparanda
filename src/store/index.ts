/**
 * comparanda/store -- persistence adapters.
 *
 * The only place in the package permitted to reach the network or browser
 * storage. Everything else talks to a port (ADR-0013).
 */
export const STORE_PLACEHOLDER = 'not yet implemented';
